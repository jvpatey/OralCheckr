// Local storage keys for habits
export enum LocalStorage {
  HABITS = "habits",
  HABITS_LOG = "habits_log",
  SELECTED_HABIT = "selected_habit",
}

// Log actions
export enum LogAction {
  ADD = "add",
  REMOVE = "remove",
}
